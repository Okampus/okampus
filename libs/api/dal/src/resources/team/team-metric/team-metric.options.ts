import type { TenantScopedOptions } from '../../tenant-scoped.options';
import type { TeamMetricProps } from '@okampus/shared/dtos';

export type TeamMetricOptions = TeamMetricProps & TenantScopedOptions;
