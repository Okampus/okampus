{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "$id": "hasura-feature",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Feature name (same as the entity name, case-insensitive)",
      "$default": {
        "$source": "argv",
        "index": 0
      }
    },
    "subfolder": {
      "type": "string",
      "description": "Subfolder to place the feature in"
    },
    "folder": {
      "type": "string",
      "description": "Folder to place the feature in"
    },
    "plural": {
      "type": "string",
      "description": "Plural name of the feature"
    },
    "expect-rels": {
      "type": "string",
      "description": "Expected relationship paths required in insert (comma-separated)"
    },
    "expect-ids": {
      "type": "string",
      "description": "Expected id relationship paths required in insert (comma-separated)"
    },
    "settles": {
      "type": "string",
      "description": "Explicit cases of settled join entities"
    },
    "tenant-scoped": {
      "type": "boolean",
      "description": "Whether the feature entity is tenant-scoped",
      "default": true
    },
    "pkColumns": {
      "type": "array",
      "description": "Primary key columns",
      "items": {
        "type": "string"
      }
    }
  },
  "required": ["name"]
}
