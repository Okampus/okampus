mutation DeleteFollow($id: bigint!) {
  deleteFollowByPk(id: $id) {
    __typename
    id
    deletedAt
    actor {
      __typename
      id
      email
      name
      slug
      website
    }
  }
}
