'use client';

// import { useTeamManage } from '../../../../../../../_context/navigation';

export default function TeamManageBankAccountInfoPage({ params }: { params: { slug: string } }) {
  // TODO trésorerie : bilan, transactions, documents comptables...
  // const { teamManage } = useTeamManage(params.slug);
  // return <div>Vos informations bancaires: {teamManage?.actor?.name}</div>;
}
