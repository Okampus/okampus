mutation UserLogin($dto: LoginInput!) {
  login(dto: $dto) {
    __typename
    canManageTenant
    user {
      __typename
      id
      slug
      tenant {
        __typename
        id
        domain
      }
    }
  }
}
