mutation UserLogin($dto: LoginInput!) {
  login(dto: $dto) {
    __typename
    canManageTenant
    user {
      __typename
      id
      slug
      tenantScope {
        __typename
        id
        domain
      }
    }
  }
}
