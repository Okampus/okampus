query GetTenantManage($domain: String!) {
  tenant(where: { domain: { _eq: $domain } }) {
    __typename
    id
    domain
    pointName
    actor {
      __typename
      id
      name
      avatar
      banner
      bio
      email
      website
      status
    }
    eventValidationForm {
      __typename
      id
      name
      schema
      type
    }
    eventApprovalSteps {
      __typename
      id
      name
      order
    }
  }
}
