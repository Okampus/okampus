mutation DeleteActorImage($where: ActorImageBoolExp!) {
  deleteActorImage(where: $where) {
    returning {
      __typename
      id
      createdAt
      type
      actor {
        __typename
        id
        email
        name
        website
      }
      image {
        __typename
        id
        url
      }
    }
  }
}
