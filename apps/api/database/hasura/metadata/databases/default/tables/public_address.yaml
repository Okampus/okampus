table:
  name: address
  schema: public
object_relationships:
  - name: createdBy
    using:
      foreign_key_constraint_on: created_by_id
array_relationships:
  - name: locations
    using:
      foreign_key_constraint_on:
        column: address_id
        table:
          name: location
          schema: public
